# ---------------------------------------------------------------------------
# 运行时依赖（Vercel Serverless 部署实际加载的最小集）。
#
# ---------------------------------------------------------------------------

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# LLM / RAG（远程 OpenAI 兼容客户端 + 文本切分 + 向量库）
langchain-openai>=0.1
langchain-text-splitters>=0.3
qdrant-client>=1.9

# Hybrid 检索的 BM25 全文侧（纯 Python，体积小）
rank-bm25>=0.2.2
jieba>=0.42.1

# Config & models
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# HTTP client（Jina rerank API backend）
httpx>=0.27

# 本地 ONNX reranker（bge-reranker-base int8 量化，CPU 推理，无 torch 依赖）
onnxruntime>=1.17
tokenizers>=0.15
huggingface_hub>=0.23
numpy>=1.26
