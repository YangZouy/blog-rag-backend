# ---------------------------------------------------------------------------
# 运行时依赖（Vercel Serverless 部署实际加载的最小集）。
#
# 线上后端走远程 API：EMBED_BACKEND=zhipu + RERANK_BACKEND=jina，
# 因此不含 torch / sentence-transformers（本地嵌入/重排才需要，见 requirements-dev.txt）。
# ingest / build_index / eval 属离线流程，其依赖也在 dev 文件里。
#
# ⚠️ Vercel 环境变量必须显式设置 RERANK_BACKEND=jina（默认值是 local，
#    否则线上会走 CrossEncoder 分支而 import 不到 sentence-transformers 直接崩）。
# ---------------------------------------------------------------------------

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# LLM / RAG（远程 OpenAI 兼容客户端 + 文本切分 + 向量库）
langchain-openai>=0.1
langchain-text-splitters>=0.3
qdrant-client>=1.9

# Hybrid 检索的 BM25 全文侧（纯 Python，体积小）
rank-bm25>=0.2.2
jieba>=0.42.1

# Config & models
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# HTTP client（Jina rerank API backend）
httpx>=0.27
