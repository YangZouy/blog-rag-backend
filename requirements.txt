# ---------------------------------------------------------------------------
# 运行时依赖（Vercel Serverless 部署实际加载的最小集）。
#
# 线上后端使用智谱 Embedding 与 Jina Reranker API。
# 因此不含 torch / sentence-transformers；后者仅用于可选本地嵌入，见 requirements-dev.txt。
# ingest / build_index / eval 属离线流程，其依赖也在 dev 文件里。
# ---------------------------------------------------------------------------

# Web framework
fastapi>=0.110
uvicorn[standard]>=0.29

# LLM / RAG（远程 OpenAI 兼容客户端 + 文本切分 + 向量库）
langchain-openai>=0.1
langchain-text-splitters>=0.3
qdrant-client>=1.9

# Hybrid 检索的 BM25 全文侧（纯 Python，体积小）
rank-bm25>=0.2.2
jieba>=0.42.1

# Config & models
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# HTTP client（Jina rerank API backend）
httpx>=0.27
