# ---------------------------------------------------------------------------
# 本地 / 离线依赖：ingest、build_index、eval、tests（Vercel 部署不安装本文件）。
# Vercel 部署不安装本文件（不在 bundle 内）。
#
# 用法：
#   pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------

# 运行时依赖照单全收
-r requirements.txt

# Ingestion：Hexo Markdown + PDF 解析
python-frontmatter>=1.0
pypinyin>=0.50
pymupdf>=1.24
pdfplumber>=0.11
requests>=2.31

# Tests
pytest>=8.0

# Offline end-to-end RAG evaluation (generation + groundedness)
ragas>=0.2
